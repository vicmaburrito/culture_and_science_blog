<div class="container-fluid ">
<div class="row h-40">
  <% @categories.each_with_index do |cat,i| %>
          <div class="col-sm">
            <%=@category.name %>
            <%=cat.title %>
            <%= read_more(cat) %>
            <%= link_to 'Read more', cat, class:'' %>
          </div>
          <div class="col-sm">
            <%= bottom_img(cat,i)%>
          </div>
  <% end %>
</div>

<div class="row h-40">
  <div class="showcase-3 col-sm"></div>
  <div class="showcase-4 col-sm"></div>
  <div class="showcase-5 col-sm"></div>
</div>
  </div>