<div class= "columns is-centered">
 <div class= "column is-half">
      <% if @current_user.nil? %>
        <h1 class="title is-1"> WELCOME! </h1>
      <%else%>
        <%= render 'feed'%>
      <% end %>

  </div>
</div>
